# Need this here in order to avoid PyTest picking up the lib's pyproject.toml
[pytest]
markers =
    driver_functionality: specifies functionality for driver to load
    skip_driver: specifies that this test should be skipped if driver has particular value
